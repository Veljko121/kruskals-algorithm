# Pokretanje programa

Program se kompajlira i pokreće komandom:
```
mkdir -p bin && g++ -o ./bin/main -fopenmp ./src/main.cpp && ./bin/main
```
Program može i da se pokrene pokretanjem skripte run.sh:
```
./run.sh
```